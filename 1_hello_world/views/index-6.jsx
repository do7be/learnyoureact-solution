let React = require('react');

let TodoBox = React.createClass({
  render: () => {
    return (
      <div className="todoBox">
        Hello, world!
      </div>
    );
  }
});

export default TodoBox;